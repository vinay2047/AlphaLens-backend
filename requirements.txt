# =============================================================================
# AlphaLens Backend — Unified Requirements
# =============================================================================
# All dependencies for the combined FastAPI backend (sentiment, price-prediction,
# shadow-portfolio). Install with:  uv pip install -r requirements.txt

# --- Core framework ---
fastapi
uvicorn[standard]
pydantic
python-dotenv

# --- Data & numerics ---
numpy
pandas
yfinance
scikit-learn
joblib

# --- Sentiment service (HuggingFace Inference API) ---
finnhub-python

# --- Price prediction service (statistical — no TensorFlow) ---
ta
httpx
websockets

# --- Shadow portfolio service (RL) ---
gymnasium
stable-baselines3
torch
matplotlib

# --- Caching (optional on Render) ---
redis
